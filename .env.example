# 复制成 .env 后填写；.env 永远不要提交（密钥不上网）
PORT=8787
# HOST 默认 0.0.0.0（手机/Expo Go 连局域网必需）；只想本机访问写 HOST=127.0.0.1
DB_PATH=./data/homeops.db
HOME_NAME=我家
# P1：语音/小票结构化用的 OpenAI 兼容接口；不填则降级为手动录入
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=
